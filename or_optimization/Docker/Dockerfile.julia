
FROM julia:1.9


WORKDIR /app

#critical packages during build phase
RUN julia -e '\
    using Pkg; \
    Pkg.add(["JuMP", "GLPK", "Redis", "JSON", "CSV", "DataFrames"]); \
    Pkg.precompile(); \
    print(" Julia Packages Locked."); \
'

CMD ["tail", "-f","/dev/null"]